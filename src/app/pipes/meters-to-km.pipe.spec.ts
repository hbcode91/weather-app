import { MetersToKmPipe } from './meters-to-km.pipe';

describe('MetersToKmPipe', () => {
  it('create an instance', () => {
    const pipe = new MetersToKmPipe();
    expect(pipe).toBeTruthy();
  });
});
